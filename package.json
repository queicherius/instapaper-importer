{
  "name": "instapaper-importer",
  "version": "1.0.1",
  "bin": {
    "instapaper-importer": "./src/index.js"
  },
  "license": "MIT",
  "scripts": {
    "format": "prettier --print-width 80 --no-semi --single-quote --no-bracket-spacing --write 'src/**/*.js' && standard --fix 'src/**/*.js'"
  },
  "dependencies": {
    "form-data": "^2.3.1",
    "minimist": "^1.2.0",
    "node-fetch": "^1.7.3",
    "progress": "^2.0.0"
  },
  "devDependencies": {
    "prettier": "^1.7.4",
    "standard": "^10.0.3"
  }
}
